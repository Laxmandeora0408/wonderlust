{
  "version": 2,
  "builds": [
    {
      "src": "app.js",  // Change this if your main entry file is different
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.js"  // Make sure this points to the correct entry point
    }
  ]
}
